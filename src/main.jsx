import React from "react";
import ReactDOM from "react-dom/client";
import PawKeeper from "./PawKeeper";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PawKeeper />
  </React.StrictMode>
);
